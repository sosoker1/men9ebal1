git clone https://github.com/sosoker1/men9ebali.github.io
cd sosoker1.github.io

echo "Hello World" > index.html
<html lang="en">
<head>
git add --all

git commit -m "Initial commit"

git push -u origin main
